import CustomSection from './CustomSection';

const Headers = () => {
  return (
    <CustomSection
      title="headers"
      attentionTranslation="attentionHeaders"
      initialActionValue={''}
    />
  );
};

export default Headers;
